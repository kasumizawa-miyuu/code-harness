# 项目反思报告

## 一、项目概述与回顾

本项目是一个轻量级、反馈闭环驱动的 Coding Agent Harness。核心等式是 Agent = LLM + Harness，即LLM 只负责"下一步做什么"的决策，而 harness 层负责将这一决策封装为可测试、可验证、可控制的工程系统。整个开发过程从 brainstorming 开始，经历多个 task 的逐步实现，再到云端部署、UI 优化、一系列 bug 修复，以及多用户会话隔离等扩展功能，最终形成了一个完整的 WebUI 可访问的编码智能体工具。

这次项目开发让我对"AI 辅助软件工程"有了更深的理解，反馈闭环、治理护栏、TDD等在实践中暴露出了它们真正的难度和深度。

---

## 二、Superpowers 技能评估

### 发挥最大作用的技能

**1. brainstorming（头脑风暴）**

这是整个项目中价值最高的技能。它没有让我直接跳进实现，而是层层追问了三个关键问题："你究竟想做什么？""选哪个方向？""为什么选这个？"。它对判据反复强调："移除真实 LLM 后，机制还能用单测验证吗？"这个追问从根本上改变了架构设计方向：从"写一个能用的 agent"变成了"写一个可测试的 harness 内核"。

在 brainstorming 阶段，它还帮我识别了"哪个维度做深"这个关键决策。六个维度（决策、工具、记忆、治理、反馈、配置）都要有最低实现，但必须选一个深入。最终选了反馈闭环——因为它是闭环的核心，天然适合用代码实现，且最容易用 mock LLM 做确定性测试。

**2. writing-plans（编写计划）**

将整个项目分解为 17 个 2-5 分钟可完成的 task，每个 task 包含明确的文件路径、接口定义、验证步骤。这种颗粒度刚好匹配 subagent 的"单次会话能力边界"。17 个 task 中，有 16 个由 subagent 一次性完成，1 个需要人工介入。

### 形式大于实质的技能

**using-git-worktrees**：worktree 的隔离本身是有价值的，每个 task 在独立分支上开发，避免相互干扰。但在本项目中，worktree 的管理开销（创建、切换、合并、清理）与收益基本持平，没有显著提升效率。对于轻量项目、单人开发者来说，普通分支 + 临时 stash 的效果相差不大。而且本项目的分支在合并后才发现没有推送到 GitHub 创建 PR，导致无法完整记录 PR 工作流——这是一个流程失误。

worktree 的真正价值可能体现在团队协作场景中，当多个开发者同时在不同 feature 上工作时，隔离才有实际意义。

---

## 三、TDD 在 AI 协作下的表现

**是阻碍还是放大器？** 我认为是放大器。

TDD 提供了一个"客观的完成标准"，这对 AI 协作至关重要。人类与 AI 协作的最大问题是"你以为它懂了，其实它没懂"。当 subagent 说"完成了"时，你无法判断它是真的完成了，还是只是输出了一段看起来像完成的代码。TDD 的红色/绿色信号给出了一个无可争议的客观判定。

---

## 四、Subagent 工作流体验

**subagent 能自主运行多久而不偏离？**

平均每个 task 约 2-5 分钟，最长的（AgentLoop 集成）约 10 分钟。至少在这个时间窗口内，subagent 没有明显出现类似"忘记上下文""重复已有代码""偏离 spec"等的问题。

**什么颗粒度最优？**

单个文件 + 单个接口 + 2-5 分钟实现时间。这个颗粒度刚好在 subagent 的"注意力跨度"内，也正好对应一次 TDD 循环（红-绿-重构）。

---

## 五、SPEC / PLAN 质量对实现质量的影响

**正面案例：** Config 模块的 SPEC 中明确定义了"环境变量覆盖优先级高于配置文件，默认值最低"。这个清晰的定义让 subagent 一次性实现了正确的逻辑，无需返工。

**反面案例：** AgentLoop 的 SPEC 中关于"自适应重试"的条件写的是"同一错误重复出现时提前停机"，但没有明确定义"同一错误"的判定标准。subagent 首次实现用了精确字符串匹配（`===`），导致测试中出现"失败信息只有微小差异但实际是不同错误"时无法正确重试。这个缺陷在 code review 阶段被发现，修正为支持更宽松的匹配策略。

**另一个反面案例：** 云端工作区的 SPEC 中描述了"用户上传 zip 后 Agent 在隔离工作区中操作"，但没有明确说明"隔离"的具体实现方式。这导致最初的实现只通过系统提示词告诉 Agent 工作区路径，而没有在代码层面强制执行。后续在线上部署时发现了严重的安全漏洞：Agent 可以访问 `/app` 目录下的服务器文件，且命令的实际执行目录是 `/app` 而非工作区。这个漏洞需要三轮修复才完全解决：先在 Guardrail 中加路径检查，再在 ToolExecutor 中加硬编码沙箱，同时修复 `execAsync` 的 `cwd` 参数缺失问题。

---

## 六、Prompt / Context 策略

**最有效的策略：** 在 subagent 的 prompt 中明确包含"输出格式"约束和"行为边界"约束。

在 WebUI 的迭代中，我们逐步优化了系统提示词。最初版本是"你必须输出 action"，后来发现 AI 会输出解释性文字（如"我先查看文件列表"），这些文字被 ActionParser 判定为无效 action，导致 Agent 陷入"输出文字 → 解析失败 → 重试 → 再次输出文字"的死循环。最终版本明确要求"Output ONLY actions — no explanations, questions, or commentary. Output exactly ONE action per response. Nothing else."这个约束让 AI 的行为变得可预测。

---

## 七、云端部署与扩展开发

在完成核心 harness 之后，项目进入了云端部署阶段。这个过程让我体验到"开发的完成"和"产品的完成"之间的巨大差距。

**云端工作区设计：** 最初的设计很简单：用户上传 zip，服务器解压，Agent 操作。但实际部署后暴露了三个持久性 bug：(1) Agent 仍然在 `/app` 目录而非工作区目录下运行命令；(2) 第二次运行任务时前端卡死；(3) 历史记录无法切换。这三个 bug 的调试过程让我深刻体会到"线上环境"和"本地测试"的差异——本地 54 个测试全部通过，但线上仍然出错，因为本地测试无法覆盖网络交互、DOM 生命周期、多用户并发等场景。

**多用户会话隔离：** 这是一个在 SPEC 阶段完全没有考虑到的需求。当课程要求提供可访问的 WebUI 时，我默认只有一个用户。但实际部署后，发现所有用户共享同一个全局 `currentSessionId` 变量，设备 B 上传工作区后会覆盖设备 A 的工作区。这个 bug 暴露了"单用户思维"在 Web 服务开发中的危险性，即任何全局状态都可能在多用户场景下出问题。修复方案是为每个浏览器分配唯一 `clientId`，将全局变量改为 `Map<clientId, sessionId>`。这个修复本身不复杂，但它让我意识到：SPEC 中缺少"并发模型"和"多用户场景"的考量是一个系统性问题。

**UI 迭代：** 前端经历了多轮修复，包括侧边栏滚动、深色/浅色模式切换、主题图标、面板折叠等。这些修复虽然不涉及核心架构，但数量之多（10+ 个 UI 问题）说明了一个事实：AI 生成的 UI 代码在"能用"和"好用"之间有很大的鸿沟。AI 能写出功能正确的 HTML/CSS/JS，但它不理解"用户体验"，比如小屏幕设备的适配、图标与主题的一致性等。这些"用户体验"细节，仍然需要人类工程师的判断和迭代。

---

## 八、Debug 过程中的教训

本项目的调试过程产生了几个值得记录的教训：

**1. 不要假设线上环境与本地一致。** 云端模式下，`process.cwd()` 是 `/app`，不是工作区目录。`execAsync` 缺少 `cwd` 参数导致所有命令在 `/app` 下执行。这个 bug 在本地测试中无法复现，因为本地测试不会模拟云端路径结构。

**2. 安全不能只靠提示词。** 最初的工作区隔离只依赖于系统提示词告诉 Agent"你的工作区目录是 XX"。但 Agent 可以（也确实会）忽略这个提示。修复方案是三层防御：提示词 + Guardrail（路径归一化检查）+ ToolExecutor（硬编码路径沙箱）。这个教训直接验证了作业的要求："机制必须是代码，不能是提示词"。

---

## 九、如果重做，我会改变什么

1. **在 SPEC 中增加"并发模型"和"多用户场景"考量。** 正如上文所述，多用户会话隔离的本应在 SPEC 阶段就想到，一个 Web 服务天然就是多用户的。这个遗漏导致了线上 bug 和紧急修复。

2. **worktree 分支应该在开发完成后立即推送到 GitHub 并创建 PR，再合并到 main。** 本项目中分支先合并后推送，导致无法创建 PR 记录工作流。这是一个应该避免的流程错误。

3. **为 UI 设计增加专门的迭代轮次。** AI 生成的 UI 代码在"能用"和"好用"之间有很大差距。应该在核心功能完成后，专门安排一轮 UI 评审和迭代，而不是在实现过程中零散地修 bug。

---

## 十、对 Superpowers 方法论的批判

**Superpowers 的核心假设：** 一个结构化的流程（brainstorming → planning → TDD → review → finish）可以驾驭 AI 的不确定性，使其产出可预测的工程结果。

**这个假设在我的项目中成立吗？** 大部分成立。TDD 和 review 的"客观完成标准"确实有效——它们提供了一个不依赖人类判断的质量门槛。但"流程"不能完全替代"判断"，流程可以保证"做对了"，但不能保证"做了对的事"。后一个问题，仍然需要人类的判断力。

**Superpowers 对安全性的假设：** 作业要求"机制必须是代码，不能是提示词"，Superpowers 也强调了这一点。本项目的 Guardrail 和 ToolExecutor 两层防线是真正的代码机制，但系统提示词中的"Do not attempt it"本质上还是提示词。虽然提示词的使用是在代码机制上的锦上添花，但在实践中还是需要警惕在代码机制和提示词之间模糊边界。

## 总结

Superpowers 是一个优秀的"流程脚手架"，它帮我守住了 TDD、评审、计划这些在 AI 协作中容易松懈的纪律。但它不能替代我对"做什么"和"做对了吗"这两个问题的回答。判断什么值得做、判断什么做对了、判断什么做错了、判断何时停止，这些决策，依然是人类的价值。

*撰写方式：本人撰写主要大体框架和内容，AI 润色文字，并对本人表述错误或过浅的部分进行修正或详细解释，最后由本人再次检查*